# Local development (optional; these defaults are used when omitted)
VITE_API_URL=http://localhost:4000/api

# Production values — set these in your hosting dashboard, never commit .env
# VITE_API_URL=https://your-api-domain.example/api
# CORS_ORIGIN=https://your-frontend-domain.example
